<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Traveler">
	<select id="getAllTraveler" resultType="www.toursAdmin.com.TravelerDto">
		SELECT * FROM TRAVELER ORDER BY REG_DATE DESC
	</select>
	
	<select id="getTravelerBySeq" resultType="www.toursAdmin.com.TravelerDto">
		SELECT * FROM TRAVELER WHERE SEQ = #{seq}
	</select>
	
	<update id="deleteTraveler" parameterType="int">
		UPDATE TRAVELER SET STATUS = 2 WHERE SEQ = #{seq}
	</update>
</mapper>
